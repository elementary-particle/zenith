extends = "default.toml"

# Full production architecture on CPU for validating audit plumbing against a
# production checkpoint. This is not a statistically useful audit profile.
[run]
profile = "cpu-smoke"

[env]
num_envs = 2
num_threads = 2

[rollout]
matches_per_update = 2

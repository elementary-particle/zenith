# 计划：编写《V18 PPO 性能优化第二轮执行提示词》文档

## 本次会话只做一件事

在 `audit/reports/v18/design/` 新建一份**自包含的执行提示词文档**（命名沿用既有惯例 `V18 PPO训练性能优化审查提示词.md` 的模式），供新会话直接投喂执行。**本会话不改任何代码、配置、tag。**

## 交付物

`audit/reports/v18/design/V18 PPO性能优化第二轮执行提示词.md`，约 10 节：

1. **背景与实测瓶颈数据**（自包含，新会话无需重新测量）：run2 iteration 14 的分解表（rollout 235s：worker RPC 等待 168.5s / update 590s：fwd 224ms + bwd 364ms ×868 批/rank）、~10% MFU 结论、推理 padding 25.8%、eval 缓存污染事实、SDPA 探针结果（mem_efficient 15.1ms vs math 78.8ms、flash 被排除）。
2. **回档点与预检**：branch `V16`、HEAD `b9af8a3`（工作区干净）；执行会话第一步 `git tag perf-v18-round2-baseline b9af8a3`；单主题 revert 与全量回档规程（含 Rust wheel 重装注意点）；预检清单（确认无训练进程、GPU 空闲、`Mahjong-AI` 环境）。
3. **范围裁定**：纳入清单与**明确排除项**（minibatch/accum、envs_per_worker、wait_ms 扫描、eval 重叠、staleness 流水重叠）——防止执行会话扩大范围；`v18_ppo.yaml` 生产配置值一律不动。
4. **语义等价性标准**：数据通路类改动必须逐位一致；批组成/后端类允许容差（给出阈值与验证脚本写法）；进攻型改动以 A/B 收益为准。
5. **阶段 A（P0，5 项，各一个 commit）**：
   - A1 eval 缓存加 checkpoint 内容校验（`train.py:483`）+ 单测
   - A2 `loss_detail` 延迟求值，消除每 minibatch 4 次 GPU 同步（`learner.py:1003-1008`）
   - A3 梯度累积 `no_sync()` + `all_reduce(MIN)` 降频（`learner.py:1011/1040`，强调双 rank collective 对齐）
   - A4 `discounted_empirical_returns` numpy 向量化（`learner.py:232-245`）+ `np.array_equal` 单测
   - A5 learner collate 跳过 `query_rows`（`learner.py:909`）
6. **阶段 B（P1 代码路径，4 项）**：
   - B1 前向去同步化：`shared_capacity`/`critic_total` 改 host 侧算好传入（`architecture.py:397/489`）；训练期关闭 `_assert_structure` 与 pair 重复校验（`:284-303/:425-457`，加配置开关，测试路径保留）
   - B2 SFT reference 每 update 预计算一次 logits（`learner.py:933-944`），minibatch 内 gather（含容差验证标准）
   - B3 SDPA 锁定 mem_efficient 后端 + 启动探针（`architecture.py:102`，防 5 倍回退悬崖）
   - B4 推理批按长度排序再 padding（`inference.py` `collate_request_rows`/`_infer_many`；输出路由按 group 顺序天然正确）
7. **阶段 C（P2 进攻型，逐项 A/B 门控、独立可回滚）**：C1 torch.compile 重试（B1 后 graph break 源已除；子模块级、`dynamic=False`、负优化即弃并记录）；C2 StateTokenEmbedding 重写（类别常量表+预计算索引，消除 argsort/tolist，~24% forward）；C3 推理 CUDA Graph（bucket 固定形状）；C4 FlexAttention（C1 成功后可选）；可选尾巴：Rust 下沉 `critic_feature_encode`。每项含放弃判据。
8. **A/B 协议**：按 AGENTS.md（`target_kl=0.0`、`update_epochs=4`、3 轮弃第 1 轮、`CUDA_DEVICE=0,1`、`learner_gpus=2`、日志落 `logs/v18/`）；新建独立自包含 `configs/v18_ppo_perftest.yaml`（只新增文件，不改生产配置）；对比指标键清单（`update/wall_s`、`timing/update/model_forward|backward/total_s`、`rollout/wall_s`、`inference/padding_fraction_of_padded_tokens` 等）。
9. **全局工程约定**：中文注释、通用性（不硬编码版本/路径/对手）、常量单源、删除前 `rg`、冒烟产物清理、归档资产只读、测试全绿命令（pytest 三包 + cargo workspace）。
10. **验收清单**：回归套件全绿、性能对比表模板、`PROGRESS.md` 记录格式。

## 预期收益（按收窄后范围，写入文档）

- 阶段 A：update −35~70s；阶段 B：update −100~180s、rollout −25~40s；阶段 C（A/B 达标才保留）：update 再 −50~120s、rollout 再 −30~60s
- 总计：`algorithm_wall_s` 825s → 约 **480~680s**（−18%~42%）

## 明确不做

不修改任何生产配置值；不做 eval 与训练重叠；不做 rollout/update 流水重叠（staleness-1）；本会话不创建 tag（由执行会话按文档第一步创建）。